{% extends "base.twig" %}

{% block content %}

    <h1>My Blog</h1>

    

    {% for post in posts %}
        <h2>{{ post.title }}</h2>
        <p style="text-transform: capitalize">by {{ post.author }}</p>
        <p>{{ post.body }}</p>
        
    {% endfor %}


{% endblock %}